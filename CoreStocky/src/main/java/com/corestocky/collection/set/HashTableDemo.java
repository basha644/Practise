package com.corestocky.collection.set;

public class HashTableDemo {
	/*
	 * In HashTable in each bucket have unique data only
	 * There is no chance to store two objects in same bucket(but when two keys hashcode ends with
	 * same int value those cases one more data structure will be created and two will store in same bucket(hash collision) )
	 * Due to this there is no chance to store duplicate objects.
	 * Default bucket size is 16 and load factor is o.75/75%
	 * 
	  */
}
